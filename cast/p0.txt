<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>Cast: chess openings with character</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Alfa+Slab+One&family=Alegreya:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
<style>
:root{--ink:#1C1F2E;--paper:#EFE2C4;--paper2:#F6ECD4;--muted:#5A4E3C;--accent:#A8322D;--sqd:#2B3350;--glow:#E6A94A;
  box-sizing:border-box;padding-top:env(safe-area-inset-top,0px);padding-bottom:env(safe-area-inset-bottom,0px)}
@media (prefers-color-scheme: dark){:root:not([data-theme="light"]){--paper:#EFE2C4}}
:root[data-theme="dark"]{--paper:#EFE2C4}
*,*::before,*::after{box-sizing:border-box}
html{scroll-padding-top:env(safe-area-inset-top,0px);background:var(--paper)}
body{margin:0;min-height:100vh;background:var(--paper);color:var(--ink);font-family:Alegreya,Georgia,serif;font-size:1.08rem;line-height:1.55;position:relative;overflow-x:hidden}
.paperfx{position:fixed;inset:0;pointer-events:none;z-index:0}
.paperfx svg{width:100%;height:100%}
.wrap{position:relative;z-index:1;max-width:1120px;margin:0 auto;padding:30px 22px 80px}
button{font:inherit;color:inherit;cursor:pointer;background:none;border:0}
button:focus-visible{outline:2px dashed var(--ink);outline-offset:4px}
.slab{font-family:"Alfa Slab One",Rockwell,"Roboto Slab",Georgia,serif;font-weight:400;line-height:.95;color:var(--paper2);
  -webkit-text-stroke:.09em var(--ink);paint-order:stroke fill;text-shadow:.05em .05em 0 var(--glow),.09em .09em 0 var(--ink);letter-spacing:.01em}

/* HOME */
.mast{text-align:center;margin:10px 0 40px}
.mast h1{margin:0;font-size:clamp(4.4rem,17vw,11rem)}
.mast p{margin:10px auto 0;max-width:34ch;font-size:1.25rem;font-style:italic;color:var(--muted)}
.group{margin-top:26px}.gh{font-family:"Alfa Slab One",Georgia,serif;font-weight:400;font-size:1.25rem;margin:0 0 18px;color:var(--muted)}
.count{font-weight:700;font-style:normal!important;font-size:1rem!important;margin-top:8px!important}
.stamp{position:absolute;top:10px;right:6px;font-family:"Alfa Slab One",Georgia,serif;font-size:.85rem;color:var(--accent);border:2.5px solid var(--accent);border-radius:8px;padding:2px 10px;transform:rotate(8deg);background:rgba(246,236,212,.85)}
.stamp.gold{color:#9A6A12;border-color:#C8902E}
.modes{flex-wrap:wrap;row-gap:10px}
.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(290px,1fr));gap:34px}
.card{text-align:left;display:flex;flex-direction:column;padding:0}
.card .art{aspect-ratio:4/3;position:relative;transition:transform .4s cubic-bezier(.2,.8,.2,1)}
.card:hover .art{transform:rotate(-1deg) scale(1.02)}
.card .art svg,.head .art svg{width:100%;height:100%;display:block;overflow:visible}
.card h2{margin:14px 0 4px;font-size:2rem;-webkit-text-stroke:.07em var(--ink);text-shadow:.04em .04em 0 var(--glow),.07em .07em 0 var(--ink)}
.card p{margin:0;font-style:italic;color:var(--muted);font-size:1.15rem}
.card .meta{margin-top:6px;font-weight:700;font-size:.95rem}

/* OPENING */
.back{font-weight:700;border-bottom:2px solid var(--ink);padding:2px 0;margin-bottom:18px}
.head{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,360px);gap:20px;align-items:center;margin-bottom:22px}
.head h1{margin:0;font-size:clamp(2.8rem,8vw,5.4rem)}
.head p{margin:12px 0 0;font-style:italic;font-size:1.3rem;color:var(--muted)}
.head .art{aspect-ratio:4/3}
.layout{display:grid;grid-template-columns:minmax(0,580px) minmax(0,1fr);gap:46px;align-items:start}
@media (max-width:860px){.layout{grid-template-columns:1fr;gap:18px}.head{grid-template-columns:minmax(0,1fr) 132px;gap:10px;align-items:end;margin-bottom:8px}.head p{font-size:1.05rem}.cap{min-height:0!important;order:2}.modes{order:0}.ctrls{order:1}.ticks{order:3}.panel{gap:16px}.mv{font-size:2.5rem!important;margin-bottom:10px!important}.cap p{font-size:1.12rem!important}}

/* BOARD */
.frame{position:relative;padding:14px 14px 34px 34px}
.under{position:absolute;inset:14px 14px 34px 34px;pointer-events:none}
.under svg{width:100%;height:100%;overflow:visible;display:block}
.board{position:relative;display:grid;grid-template-columns:repeat(8,1fr);grid-template-rows:repeat(8,1fr);aspect-ratio:1/1;width:100%;user-select:none;-webkit-user-select:none}
.sq{position:relative;min-width:0;min-height:0}
.sq.last::before{content:"";position:absolute;inset:6%;background:var(--glow);opacity:.55;border-radius:38% 45% 40% 50%;filter:blur(2px)}
.sq.sel::before{content:"";position:absolute;inset:4%;border:3px solid var(--glow);border-radius:40% 48% 44% 50%}
.sq.hint::after{content:"";position:absolute;inset:4%;border:3px dashed var(--accent);border-radius:50%;z-index:4;animation:spin 7s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.dot{position:absolute;left:38%;top:38%;width:24%;height:24%;border-radius:45% 55% 50% 48%;background:var(--glow);opacity:.85;z-index:3;pointer-events:none;box-shadow:0 0 0 2px var(--ink)}
.ring{position:absolute;inset:3%;border-radius:50%;border:3px solid var(--glow);z-index:3;pointer-events:none}
.rank,.file{position:absolute;font-family:"Alfa Slab One",Georgia,serif;font-size:.8rem;color:var(--muted);pointer-events:none}
.rank{left:-24px;top:50%;transform:translateY(-50%)}
.file{bottom:-28px;left:50%;transform:translateX(-50%)}
.pc{position:absolute;inset:5% 8% 7%;z-index:2;filter:drop-shadow(2px 3px 0 rgba(28,31,46,.28))}
.pc svg{width:100%;height:100%;display:block;overflow:visible}
.board.shake{animation:shake .3s}
@keyframes shake{33%{transform:translateX(-5px) rotate(-.4deg)}66%{transform:translateX(5px) rotate(.4deg)}}

/* PANEL */
.panel{display:flex;flex-direction:column;gap:22px;padding-top:6px}
.modes{display:flex;gap:26px}
.modes button{font-family:"Alfa Slab One",Georgia,serif;font-size:1.05rem;color:var(--muted);padding-bottom:6px;position:relative}
.modes button[aria-pressed="true"]{color:var(--ink)}
.modes button[aria-pressed="true"]::after{content:"";position:absolute;left:-4px;right:-4px;bottom:0;height:8px;background:var(--accent);opacity:.85;border-radius:40% 60% 50% 45%;transform:rotate(-1deg)}
.cap{min-height:220px}
.mv{margin:0 0 16px;font-size:clamp(3rem,7vw,4.6rem)}
.cap p{margin:0;font-size:1.3rem;max-width:34ch}
.fb{margin-top:14px!important;font-size:1.05rem!important;font-style:italic;color:var(--muted)}
.ticks{display:flex;gap:4px}
.ticks i{flex:1;height:10px;background:rgba(28,31,46,.14);border-radius:40% 50%}
.ticks i.on{background:var(--accent)}
.ctrls{display:flex;gap:12px;flex-wrap:wrap}
.btn{font-family:"Alfa Slab One",Georgia,serif;padding:10px 20px;border:2.5px solid var(--ink);border-radius:6px 10px 7px 12px;background:var(--paper2);box-shadow:3px 3px 0 var(--ink);transition:transform .1s}
.btn:active:not(:disabled){transform:translate(2px,2px);box-shadow:1px 1px 0 var(--ink)}
.btn:disabled{opacity:.35;cursor:default}
.btn.primary{background:var(--accent);color:var(--paper2)}
@media (prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}
</style>
</head>
<body>
<svg width="0" height="0" style="position:absolute" aria-hidden="true">
<defs>
<filter id="wob" x="-5%" y="-5%" width="110%" height="110%"><feTurbulence type="fractalNoise" baseFrequency=".035" numOctaves="2" seed="4"/><feDisplacementMap in="SourceGraphic" scale="9"/></filter>
<filter id="ink" x="-10%" y="-10%" width="120%" height="120%"><feTurbulence type="fractalNoise" baseFrequency=".5" numOctaves="2" seed="2" result="t"/><feDisplacementMap in="SourceGraphic" in2="t" scale="1.3"/></filter>
<filter id="wash" x="-40%" y="-40%" width="180%" height="180%"><feTurbulence type="fractalNoise" baseFrequency=".022" numOctaves="4" seed="9" result="t"/><feDisplacementMap in="SourceGraphic" in2="t" scale="90" result="d"/><feGaussianBlur i